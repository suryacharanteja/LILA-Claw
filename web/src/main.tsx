import React from "react";
import { createRoot } from "react-dom/client";
createRoot(document.getElementById("root")!).render(<main><h1>LILA Claw</h1><p>M0 foundation build. Task execution is not available yet.</p></main>);
